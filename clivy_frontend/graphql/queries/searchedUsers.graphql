query SearchedUsers {
  searchedUsers {
    # userId,
    searchedUserId,
    searchedUser {
      id
      username
      name,
      file {
        file
        error
      }
    }
    updatedAt
	}
}